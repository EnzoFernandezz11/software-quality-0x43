name: Horusec Security Scan

on:
  pull_request:
  push:

permissions:
  contents: read

jobs:
  scan:
    name: Scan repository
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run Horusec
        run: |
          docker run --rm \
            -v /var/run/docker.sock:/var/run/docker.sock \
            -v "${GITHUB_WORKSPACE}:/src" \
            horuszup/horusec-cli:v2.9.0-beta.3 \
            horusec start \
              --project-path /src \
              --container-bind-project-path "${GITHUB_WORKSPACE}" \
              --output-format json \
              --json-output-file /src/horusec-results.json

      - name: Upload Horusec JSON report
        uses: actions/upload-artifact@v4
        with:
          name: horusec-json-${{ github.sha }}
          path: horusec-results.json
          if-no-files-found: error
          retention-days: 14
